Map: {{model.name}}

<ul>
{{#each model.markers as |marker|}}
<li>{{marker.name}}, {{marker.cM}}</li>
{{/each}}
</ul>

<div class="btn-group">
  <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
    Add map
    <span class="caret"></span>
  </button>
  <ul class="dropdown-menu" role="menu">
  {{#each controllers.mapview.availableMaps as |mymap|}}
  <li>
    {{#link-to "mapview.map" mymap}}
      {{mymap.name}}
    {{/link-to}}
  </li>
  {{/each}}
  </ul>
</div>

Available maps: {{#each mapviewController.availableMaps as |map|}}
{{map.name}}
{{/each}}

{{outlet}}
